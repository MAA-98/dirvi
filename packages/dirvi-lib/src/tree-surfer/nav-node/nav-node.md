# Design

## Motivation

The nav node integrates the node tree and fold tree to one navigation tree,
for both rendering and navigating the UI. It interprets the fold tree in the
context of the node tree.

The API on that navigation tree can then make UI-aware methods.

